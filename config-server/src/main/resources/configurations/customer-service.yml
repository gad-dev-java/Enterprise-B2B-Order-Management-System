spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/customer_service_db
    driver-class-name: org.postgresql.Driver
    username: gad-dev
    password: 123456789
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      register-with-eureka: true
      fetch-registry: true
    instance:
      prefer-ip-address: true
      instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

server:
  port: 8083
  servlet:
    context-path: /api/v1

app:
  keycloak:
    server-url: http://localhost:8180
    realm: b2b-services]
    client-secret: kaUcD8uYAAvvMyBXhddyId2L0YgJ59Ao
    client-id: b2b-services-api
    connection-pool-size: 5
    connect-timeout: 10
    read-timeout: 30
    verify-connection: true